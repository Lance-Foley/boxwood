# ws tmux configuration
# Sourced by ws when creating sessions — also works with: tmux source-file /path/to/tmux.conf

# Active pane border highlight
set-option -g pane-active-border-style 'fg=cyan,bold'
set-option -g pane-border-style 'fg=colour238'
